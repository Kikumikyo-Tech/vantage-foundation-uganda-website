// Test stub for Next.js's `server-only` package.
// In production this package throws when imported from a client component,
// preventing server-only code from leaking to the browser. In unit tests we
// just want it to be a no-op so we can exercise the pure helpers in modules
// that import it.
export {};
